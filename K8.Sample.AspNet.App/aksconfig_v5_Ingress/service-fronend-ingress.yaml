apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: k8-ingress
  namespace: ingress-nginx
spec:
  rules:
  - host: localhost
    http:
      paths:
      - backend:
          service:
            name:  k8-fronend-cluster-service
            port:  6001